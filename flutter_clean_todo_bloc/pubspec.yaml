name: clean_architecture_templates
description: A new Flutter application.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.3
  bloc: ^6.0.3
  flutter_bloc: ^6.0.5
  equatable: ^1.2.5
  dartz: ^0.9.1
  get_it: ^4.0.4
  http: ^0.12.2
  device_preview: ^0.4.8

dev_dependencies:
  flutter_test:
    sdk: flutter


# The following section is specific to Flutter.
flutter:
  uses-material-design: true
